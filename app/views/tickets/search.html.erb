<% if @tickets_count > 0 %>
	<%= link_to "Tickets (#{@tickets_count})", search_tickets_path(:search => params[:search]) %>
<% else %>
	Tickets (0)
<% end %>
<br />
<% @tickets_searched.each do |ticket| %>
	<%= ticket.title %><br />
<% end %>