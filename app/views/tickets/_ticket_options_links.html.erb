<div class="btn-group pull-right">
  	<button class="btn">Options</button>
  	<button class="btn dropdown-toggle" data-toggle="dropdown">
    	<span class="caret"></span>
  	</button>
  	<ul class="dropdown-menu">
      <li>
        <%= link_to 'Edit Ticket', edit_project_ticket_path(ticket.project_id, ticket) %>
      </li>
      <li>
        <%= link_to 'Destroy', project_ticket_path(ticket.project_id, ticket), method: :delete, data: { confirm: 'Are you sure?' } %>
      </li>
    </ul>
 </div>

