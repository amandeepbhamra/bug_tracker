<%- model_class = Ticket -%>

<div class="row-fluid">
  	<div class="span4">
  		<%= render 'links_status_tickets' %>
  	</div>
  	<div class="span4 pull-right">
  		<%= render 'search_form' %>
  	</div>
</div>

<%= render 'user_assigned_tickets' %>

<div class="form-actions">
 
</div>