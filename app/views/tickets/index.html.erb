<%- model_class = Ticket -%>

<% content_for :sidebar do %>
	<%#= link_to 'New Ticket', new_ticket_by_mentioning_project_tickets_path %>
<% end %>

<div class="row-fluid">
  	<div class="span4">
  		<%= render 'links_status_tickets' %>
  	</div>
  	<div class="span4 pull-right">
  		<%= render 'search_form' %>
  	</div>
</div>

<%= render 'user_assigned_tickets' %>

