<tr>
    <td>
    	<strong>
    		<%= link_to project.title, project_path(project) %>
    	</strong>
    </td>
    <td><%= project.description %></td>
    <td><%= link_to_if(project.user_id == current_user.id , "New Ticket", 
    		new_project_ticket_path(project), :class => "btn btn-info") %>
    </td>
</tr>

