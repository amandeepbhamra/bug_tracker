<tr>
    <td>
    	<strong>
    		<%= link_to project.title, user_project_path(project.user_id, project) %>
    	</strong>
    </td>
    <td><%= project.description %></td>
    <td><%= link_to_if(project.user_id == current_user.id , "New Ticket", new_user_project_ticket_path(@user, project), :class => "btn btn-info") %>
    </td>
</tr>

